<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">

    <t t-name="web.X2ManyPlusField" >
        <div t-att-class="className" style="height:580px">
            <div class="o_x2m_control_panel d-empty-none mb-4">
                <t t-if="displayControlPanelButtons">
                    <div class="o_cp_buttons gap-1" role="toolbar" aria-label="Control panel buttons" t-ref="buttons">
                        <t t-foreach="creates" t-as="create" t-key="create_index">
                            <button
                                t-if="create.type === 'create'"
                                type="button"
                                class="btn btn-secondary"
                                t-att-class="create.class"
                                t-on-click.stop.prevent="() => this.onAdd({ context: create.context })"
                            >
                                <t t-esc="create.string"/>
                            </button>
                            <ViewButton
                                t-if="create.type === 'button'"
                                className="`${create.className}`"
                                clickParams="create.clickParams"
                                icon="create.icon"
                                record="props.record.data[props.name]"
                                string="create.string"
                                title="create.title"
                            />
                        </t>
                    </div>
                </t>
                <div t-if="props.record.data[props.name].count > props.record.data[props.name].limit" class="o_cp_pager" role="search">
                    <Pager t-props="pagerProps"/>
                </div>
            </div>
            <div t-if="filter_fields.length" class="x2many_plus_search_panel" t-ref="search_panel">
                <t t-foreach="filter_fields" t-as="field" t-key="field_index">
                    <span class="m-2" t-esc="field.string + ':'"/>
<!--                    <input class="o_input" t-att-name="field.name" t-on-keydown="subTreeReload"/>-->
                    <Field name="field.name" record="default_record" fieldInfo="field" readonly="false"/>
                </t>

                <button style="min-width:50px" class="btn btn-secondary m-2" type="button" t-on-click="subTreeReload">
                    查询
                </button>

                <button t-if="props.crudOptions.print" t-esc="props.crudOptions.printName" style="min-width:50px"
                        class="btn btn-primary m-2" type="button" t-on-click="printOnline">
                </button>

                <button t-if="props.crudOptions.export" t-esc="props.crudOptions.exportName" style="min-width:50px"
                        class="btn btn-primary m-2" type="button" t-on-click="exportOnline">
                </button>

            </div>
            <ListRenderer t-if="props.viewMode === 'list'" t-props="rendererProps"/>
            <KanbanRenderer t-elif="props.viewMode" t-props="rendererProps" />
        </div>
    </t>

</templates>
